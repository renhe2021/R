# 2026-03-10 工作日志

## 修复：LLM API 请求频率超限导致投委会辩论全部失败

### 问题
运行 Pipeline Stage 7d（投委会辩论）时，12 个 Agent × 8 只股票 = 96 个 LLM 请求同时并发打到 `dev.fit-ai.woa.com` 代理 API，触发频率限制（error code 1620867020：`请求频率超出限制`）。所有模型（claude-sonnet-4-5、gemini-3.1-pro、gpt-5、deepseek-v3.2）全部失败，所有 Agent 降级为 HOLD (10%) fallback。

### 同时发现的另一个 Bug
`gpt-5` 模型降级时报错 `AsyncCompletions.create() got an unexpected keyword argument 'max_completion_tokens'`。原因：`_adapt_kwargs_for_model()` 把 `max_tokens` 重命名为 `max_completion_tokens`，但代理 API 不认识该参数。

### 修复方案

#### 1. 三层并发控制（`llm.py`）
- **Semaphore（全局）**：`LLM_MAX_CONCURRENCY = 3`，限制最大同时 3 个 LLM 请求在飞
- **请求间隔（全局）**：`LLM_MIN_INTERVAL_MS = 200ms`，两次请求之间最少间隔 200ms
- **rate limit 检测增强**：HTTP 400 中含"频率超出限制"时自动重试（等 3×attempt 秒），而非直接跳到下一模型

#### 2. 股票级并发限制（`unified_pipeline.py`）
- Stage 7d（投委会辩论）：最多 2 只股票同时辩论（原来 8 只同时）
- Stage 7e（LLM 深度分析）：最多 2 只股票同时分析（原来 8 只同时）

#### 3. Agent 级并发限制（`debate.py`）
- 每只股票的 12 个 Agent 不再全部同时启动
- `agent_semaphore = Semaphore(3)`：每只股票最多 3 个 Agent 同时调用 LLM

#### 4. GPT-5 参数修复（`llm.py`）
- `_adapt_kwargs_for_model()`：不再把 `max_tokens` 改名为 `max_completion_tokens`
- 代理 API 统一使用 `max_tokens`，只移除 GPT-5 不支持的 `temperature`/`top_p`

### 并发效果对比
| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| 最大瞬时 LLM 请求 | 96（12 agent × 8 stock） | 3（全局 Semaphore） |
| 股票级并发 | 8 只同时 | 2 只同时 |
| Agent 级并发 | 12 个同时 | 3 个同时 |
| 请求间隔 | 0ms | ≥200ms |

### 修改文件
- `backend/app/agent/llm.py` — 添加 Semaphore + 请求间隔 + rate limit 检测 + GPT-5 修复
- `backend/app/agent/unified_pipeline.py` — Stage 7d/7e 股票级并发限制
- `backend/app/agent/committee/debate.py` — Agent 级并发限制

## 新功能：Pipeline 数据质量检查点 (Data Quality Checkpoint)

### 需求
用户希望 Pipeline 不要一步到底，在数据采集（Stage 2）完成后暂停，让用户能够检查数据质量、覆盖率、关键指标正确性等，确认无误后再继续后续分析。

### 设计
使用已有的 SSE Pause-Resume 模式（与 `data_source_required` 相同）：

1. Pipeline Stage 2 完成后，构建详细的数据质量报告
2. 缓存 Pipeline 状态到 `_checkpoint_cache`（class-level dict）
3. 发射 `checkpoint` SSE 事件 → Pipeline `return`（暂停）
4. Frontend 弹出模态框，显示逐股数据详情
5. 用户点击"继续" → Frontend 重新提交 Pipeline 请求，带 `resumeFrom: 3` + `resumeRunId`
6. Pipeline 从缓存恢复状态，从 Stage 3 继续

### 数据质量报告内容（`_build_data_checkpoint()`）
- **Summary**: 总股票数、获取成功数、已淘汰数、平均核心覆盖率、数据源、策略
- **Per-stock details**:
  - 状态（ok/warning/poor/eliminated）
  - 核心/扩展/历史 覆盖率百分比
  - 关键指标（PE、PB、ROE、D/E、市值、EPS、营收、FCF 等）
  - Stage 3 门槛淘汰预览（该股票会不会通过 Stage 3？）
  - 缺失核心字段列表
- **Warnings**: 数据获取失败、低覆盖率、Stage 3 淘汰风险

### 修改文件
- `backend/app/agent/unified_pipeline.py` — `run()` 添加 `resume_from`/`resume_run_id` 参数；`_checkpoint_cache`；`_build_data_checkpoint()` 方法
- `backend/app/api/agent_routes.py` — `PipelineRequest` 添加 `resumeFrom`/`resumeRunId` 字段；传递给 `pipeline.run()`
- `web/index.html` — 添加 checkpoint 模态框 HTML
- `web/assets/style.css` — 添加 checkpoint 模态框样式（表格、覆盖率条、警告等）
- `web/assets/app.js` — `handlePipelineEvent` 处理 `checkpoint`/`checkpoint_resumed`；`showCheckpointModal()`、`continueFromCheckpoint()`、`abortCheckpoint()` 函数

## 数据层深度改造（A+B+C 三个模块）

### 模块 A: Pipeline 内数据复用（消除重复 fetch）
**问题**：每只股票在一次 Pipeline 中被 `provider.fetch()` 4-5 次（Stage 2/4/5/6 各一次），30 只股 = 120+ 次无意义 API 调用。

**修复**：
- `tools.py`：`detect_shenanigans()` 和 `run_full_valuation()` 增加 `stock_data=None` 参数，传入时跳过 fetch
- `unified_pipeline.py`：Stage 4 和 Stage 6 从 `data_map` 取 stock 传入（Stage 5 已用 data_map）

**效果**：每只股票 API 调用从 4-5 次降到 1 次，预计耗时减少 ~50%

### 模块 B: 磁盘缓存层（跨次运行复用）
**问题**：修改策略重跑时所有数据从头下载，财报数据 8h 内不变。

**方案**：
- 新增 `src/data_providers/cache.py` — `CachingProvider` 装饰器
  - 包装任何 DataProvider，按 `{symbol}_{provider}_{date}.json` 序列化到 `data/cache/`
  - 默认 TTL=8h，自动清理 3 天前的缓存
  - 提供 `invalidate(symbol)` / `invalidate_all()` / `get_cache_info(symbol)` 方法
- `src/analyzer.py` — `StockData` 增加 `from_dict()` 反序列化 + `_fetched_at` 时间戳
- `src/data_providers/factory.py` — `get_data_provider()` 自动用 `_wrap_with_cache()` 包装所有 provider
- `.gitignore` — 添加 `data/cache/`

### 模块 C: Checkpoint 报告增强
在已有的 `_build_data_checkpoint()` 基础上增加 4 个新维度：

1. **历史数据深度** — EPS 年数、分红年数、是否有 10 年均值、盈利年数、连续盈利年数
2. **数据新鲜度** — 获取时间、是否缓存、缓存年龄
3. **数据异常检测** — PE>200、PE<0、D/E<0、D/E>5、FCF 与利润背离、ROE>100%
4. **Stage 3 淘汰原因详解** — 结构化的 {gate, actual, threshold, severity, suggestion}

前端增强：
- `index.html` — 表格新增"历史深度"和"新鲜度"列
- `app.js` — 渲染历史深度指示器（📊/⚠️/❌）、新鲜度指示（🔄/💾）、异常高亮、详细淘汰 tooltip
- `style.css` — `.ckpt-anomaly` 红色高亮样式

### 修改文件清单
- `backend/app/agent/tools.py` — detect_shenanigans/run_full_valuation 接受 stock_data
- `backend/app/agent/unified_pipeline.py` — Stage 4/6 传入 data_map + checkpoint 增强
- `src/analyzer.py` — StockData.from_dict() + _fetched_at
- `src/data_providers/cache.py` — 新文件：CachingProvider
- `src/data_providers/factory.py` — 缓存集成
- `web/index.html` — 新增列
- `web/assets/app.js` — 增强 showCheckpointModal
- `web/assets/style.css` — anomaly 样式
- `.gitignore` — data/cache/

## 修复：Bloomberg 连接风暴（每次 Stage 2 反复连接断开 16+ 次）

### 问题
用户运行 Pipeline 时日志显示 Bloomberg Terminal 在 Stage 2 数据采集期间反复连接/断开约 16 次（10 秒内）。

### 根因分析
1. `_stage2_data_acquisition()` → `_fetch_single()` 对每只股票调用 `get_data_provider("bloomberg")`
2. 每次 `get_data_provider()` 创建新的 `CachingProvider(BloombergProvider())` 实例
3. 每个 `BloombergProvider` 实例创建独立的 blpapi session（开连接）
4. 临时 provider 被 GC 时 `__del__` 调用 `close()`（关连接，打日志"连接已关闭"）
5. 同时 `probe_bloomberg_first()` 也创建独立的 `BloombergProvider` 实例（又一个 session）
6. `tools.py` 的 `scan_history()` 直接 `BloombergProvider()` 绕过 factory（又一个 session）

### 修复方案（4 层防护）

#### 1. Provider 单例模式（`factory.py`）
- 新增 `_provider_instances` dict + `_provider_lock` 线程锁
- `_get_or_create_provider(key, factory_fn)` — 线程安全的单例获取
- `get_data_provider()` 所有分支改为走单例路径：同一 `provider_name` 只创建一次
- `clear_provider_cache()` — 显式清理 provider（关闭连接）
- `_wrap_with_cache()` 增加 `isinstance(CachingProvider)` 判断防重复包装

#### 2. Stage 2 共享 provider（`unified_pipeline.py`）
- provider 在循环外创建一次 `shared_provider = get_data_provider(chosen_source)`
- `_fetch_single()` 通过闭包复用 `shared_provider`，不再每只股票创建新实例

#### 3. Bloomberg `__del__` 静默化（`bloomberg.py`）
- `close()` 改为只在显式调用时打日志："连接已关闭（显式调用）"
- `__del__` 改为静默关闭 session，不打日志，避免连接风暴假象
- 单例模式下 `BloombergProvider` 被 `_provider_instances` 持有，不会被 GC

#### 4. 消灭绕过 factory 的直接构造
- `tools.py` `scan_history()`：`BloombergProvider()` → `get_data_provider("bloomberg")`（走单例）
- `factory.py` `get_all_available_providers()`：直接构造 → `get_data_provider("bloomberg")`
- `probe_bloomberg_first()`：直接构造 → `get_data_provider("bloomberg", **kwargs)`（走单例）

### 效果对比
| 场景 | 修复前 | 修复后 |
|------|--------|--------|
| Stage 2 Bloomberg session 数 | N（每只股票 1 个） | 1（共享单例） |
| probe 额外 session | 1 | 0（复用单例） |
| scan_history 额外 session | 1 | 0（复用单例） |
| GC 触发的"连接已关闭"日志 | N 次 | 0 次（静默化） |

### 修改文件
- `src/data_providers/factory.py` — 单例模式 + get_data_provider 重构 + probe 重构
- `src/data_providers/bloomberg.py` — __del__ 静默化 + close() 显式标记
- `backend/app/agent/unified_pipeline.py` — Stage 2 shared_provider
- `backend/app/agent/tools.py` — scan_history 走 factory 单例

## 修复：/api/books 404 Not Found

### 问题
页面加载时 `loadBookFilter()` 请求 `GET /api/books`，返回 404。该路由仅存在于旧 Flask 版 `src/web_app.py`，FastAPI 后端未注册。

### 修复
- 新增 `backend/app/api/knowledge_routes.py`，提供 3 个端点：
  - `GET /api/books` — 列出 `data/knowledge/` 下的已导入书籍
  - `GET /api/rules/{book_name}` — 获取某本书的规则详情
  - `POST /api/cross-validate` — 多源交叉验证
- `backend/app/main.py` — 注册 `knowledge_router` 到 `/api` 前缀

## 修复：Bloomberg 并发数据串线（AAPL 数据为空 + 股票名称张冠李戴）

### 问题
Checkpoint 截图显示：
- AAPL 核心覆盖率 0%，所有指标为空
- MSFT 名字显示为 "PepsiCo Inc"，GOOGL 显示 "NVIDIA Corp"，AMZN 显示 "Tesla Inc"
- 数据张冠李戴，股票之间的数据被交叉分配

### 根因
上一个修复（Provider 单例）让所有股票共享同一个 `BloombergProvider` 实例（同一个 blpapi session），但 `_stage2_data_acquisition()` 通过 `run_in_executor` 并行获取多只股票。

blpapi session 的 `sendRequest()` / `nextEvent()` **不支持多线程并发**——Thread A 发送 AAPL 请求后，Thread B 的 `nextEvent()` 可能拿到 AAPL 的响应，导致数据串线。

### 修复
- `bloomberg.py`: `__init__` 添加 `self._lock = threading.Lock()`
- `fetch()` 改为 `with self._lock: return self._fetch_locked(symbol)`
- 确保同一时刻只有一个线程在执行 BDP/BDH 请求
- 性能影响：Bloomberg fetch 变为串行，但 CachingProvider 磁盘缓存保证第二次运行无影响

### 附加修复：CachingProvider 脏数据保护
- `cache.py` `fetch()`: 缓存前检查核心字段数量，< 3 个核心字段（price/pe/eps/market_cap/revenue/roe/pb/name）则跳过缓存，防止保存串线脏数据
- `cache.py` `_cache_path()`: `safe_symbol` 现在用 `re.sub(r'[/\.\s]+', '_', ...)` 替换所有不安全字符（包括空格），文件名从 `AAPL US Equity_bloomberg_...` 变为 `AAPL_US_Equity_bloomberg_...`
- **需要手动清除旧缓存**：`data/cache/` 目录下的 .json 文件可能是之前并发串线时的脏数据，需要全部删除后重新运行

## 缓存策略简化：关闭磁盘缓存 + 基准数据内存缓存

### 决策背景
用户反馈 AAPL 数据持续为空，根因是磁盘缓存保存了并发串线时的脏数据。用户建议"数据量不大可以每次都重新下载"。

### 分析
- 每只股票 Bloomberg fetch = 4 次 API 调用（1 BDP 47字段 + 1 BDH 8字段×10年 + 2 基准 BDP），~5-6秒/股
- 30只串行 ≈ 2.5-3 分钟，数据量不大
- Pipeline 内同次运行的数据复用（Module A: data_map 传递 Stage 4/5/6）已有效，不依赖磁盘缓存
- 磁盘缓存 JSON 方案在并发场景下脆弱（已被证实）

### 改动
1. **`factory.py`**: `_CACHE_ENABLED = False`（默认关闭磁盘缓存）
   - Pipeline 每次运行都从 Bloomberg/yfinance 重新获取，保证数据新鲜
   - data_map 内存传递依然有效（Stage 4/5/6 不重复 fetch）
   - 保留 CachingProvider 代码和 `set_cache_enabled()` API，用户可按需开启
2. **`bloomberg.py`**: 基准数据内存缓存（class-level `_benchmark_cache`）
   - SPX PE 和国债利率一天只变一次
   - 30只股票共享同一份基准数据，从 60 次查询降到 2 次
   - 用日期作为缓存 key，跨日自动失效

### 修改文件
- `src/data_providers/factory.py` — `_CACHE_ENABLED = False`
- `src/data_providers/bloomberg.py` — `_benchmark_cache` + `_fetch_market_benchmarks()` 改用缓存

### 未来规划（P2）
- SQLite 数据库 + 定时拉取任务（cron/APScheduler）
- 存储结构化的 StockData 表，带版本控制和更新时间戳
- 支持增量更新（只拉取变化的字段）

## 新功能：阈值优化 & Walk-Forward 前端 UI

### 背景
后端已有三个新 API 端点（`/agent/backtest/optimize`、`/agent/backtest/walk-forward`、`/agent/backtest/apply-optimized`），需要配套前端页面。

### 实现

#### HTML (`web/index.html`)
在 `<section id="page-backtest">` 页末尾（回测结果区之后）添加：
- **分隔线**：`opt-section-divider` 带渐变线和标签
- **阈值优化配置面板**：股票池输入（可复用回测输入）、4 个预设按钮、搜索方式/最大试验/持仓周期/回溯年数/训练比例/策略 6 个参数、12 个可选参数 tag
- **两个操作按钮**：紫色"开始阈值优化" + 绿色"Walk-Forward 验证"
- **Walk-Forward 额外配置**：折数/训练窗口/测试窗口/每折最大试验（点击按钮展开）
- **进度区**：标题 + 阶段标签 + 进度条 + 文本 + 试验计数器
- **优化结果区**：Baseline vs Best 对比卡、最优参数表、试验排行表、应用按钮
- **Walk-Forward 结果区**：4 个汇总卡（平均/最小/最大 OOS Sharpe + 结论）、各折详情卡、共识参数表、应用按钮

#### CSS (`web/assets/style.css`)
新增 ~380 行样式，覆盖：
- 分隔线、配置面板、参数标签选择器、渐变操作按钮
- 进度条（紫色 for 优化，绿色 for WF）
- Baseline vs Best 对比卡（顶部彩色条）
- 参数表、试验排行表（带最优高亮）
- 应用成功状态
- Walk-Forward 汇总卡、折卡、共识参数表
- 响应式布局

#### JavaScript (`web/assets/app.js`)
新增 ~400 行逻辑：
- `runOptimization()` — POST `/agent/backtest/optimize`，SSE 流式读取
- `handleOptimizeEvent()` — 处理 `optimize_phase`/`optimize_progress`/`optimize_baseline`/`optimize_complete`/`optimize_error`
- `renderOptimizationResults()` — 渲染对比卡 + 参数表 + 试验排行 + 应用按钮
- `runWalkForward()` — POST `/agent/backtest/walk-forward`，SSE 流式读取（首次点击展开额外配置，再次点击运行）
- `handleWalkForwardEvent()` — 处理 `walkforward_phase`/`walkforward_fold_start`/`walkforward_progress`/`walkforward_fold_complete`/`walkforward_complete`/`walkforward_error`
- `renderWalkForwardResults()` — 渲染汇总卡 + 各折详情 + 共识参数 + 应用按钮
- `applyOptimizedParams(source)` — POST `/agent/backtest/apply-optimized`，应用后显示成功状态
- 参数 tag 点击选择/取消、preset 按钮绑定、`_getSelectedParamKeys()`、`_getOptStocks()` 辅助函数

### 所有文件 Lint 检查通过（0 错误）

## 改造：阈值优化引擎 — 10年回溯 + 固定3年测试期 + Grid Search 增强

### 需求
用户要求回测验证期（test）至少3年。例如有10年数据，前7年用 Grid Search 搜索最优阈值，后3年验证整套选股逻辑的有效性。

### 核心改动

#### 1. ThresholdOptimizer — 固定测试年数
- 新参数 `test_years`（默认3.0）：固定最后N年作为测试集
- Train/Test 按日期截断而非比例分割
- 默认值：`lookback_years=10`、`max_trials=200`、`search_method="grid"`

#### 2. Grid Search 增强
- 自适应步数（≤3参数→7步, 4-6→5步, 7+→4步）
- Grid 始终包含当前值

#### 3. HistoricalDataFetcher — 长回溯支持
- Snapshot fallback：超出财报范围时用最早可用财报 + 精确价格
- 新函数 `_get_earliest_col()`

#### 4. API 层
- `lookbackYears` 范围 3-20 年，默认 10
- 新增 `testYears` 参数（2-10 年，默认 3）
- WF `testWindowMonths` 默认 36（3年）

#### 5. 前端 UI
- 回溯年数 [3,5,7,10,15]，默认 10
- "训练集比例"→"测试期年数" [2,3,4,5]，默认 3
- 搜索方式默认 Grid

### 修改文件
- `backend/app/agent/backtest/threshold_optimizer.py`
- `backend/app/agent/backtest/walk_forward.py`
- `backend/app/agent/backtest/historical_data.py`
- `backend/app/api/agent_routes.py`
- `web/index.html`
- `web/assets/app.js`

## 改造：财报分析框架自适应修复（方案 A+B+C）

### 问题总结
回测引擎存在 5 个关键的数据约束与规则设计不匹配问题：
1. F-Score/Z-Score/M-Score 在 snapshot 中声明但从未计算 → Stage 4 排雷完全无效
2. `earnings_growth_10y` 声明但从未赋值 → Buffett "10年CAGR>3%"规则永远 skip
3. TTM 年化用 2-3 个季度直接累加（不缩放） → 低估收入/利润约 33-50%
4. `market_pe` 缺失 → Quantitative "PE<市场平均"规则永远 skip
5. `profitable_years` 最大值 4-5 vs Graham/Buffett 要求 10+ → 规则永远 fail

### 方案 A：计算所有可从现有数据推导的指标

**新增 3 个计算函数**（`historical_data.py`）：
- `_compute_f_score()` — Piotroski F-Score (0-9)，9 个二元指标，从 BS/IS/CF 季度数据计算
- `_compute_z_score()` — Altman Z-Score (5因子)，从 BS/IS + market_cap 计算
- `_compute_m_score()` — Beneish M-Score (8变量)，需要 YoY 比较，从 2 年季度数据计算

**新增计算**（`build_snapshot()`）：
- `earnings_growth_10y` — 用 annual_eps_list 首尾计算 CAGR
- `market_pe` — 从 benchmark (SPY) 的 PE 提取，在 `fetch_all()` 后注入所有股票

### 方案 B：自适应降级长历史规则

**新增函数**（`historical_screener.py`）：
- `_adaptive_degrade()` — 在规则评估前根据 `available_years` 动态调整数据
  - 如果盈利率 ≥ 90%（如 4/4 年盈利），等比缩放 profitable_years 到 10
  - 如果盈利率 ≥ 70%，缩放到 8
  - dividend_years 为 None 时保持 None（skip 而非 fail）

### 方案 C：TTM 安全阀

**修改**（`build_snapshot()` 中 TTM 年化逻辑）：
- 收入/利润/EPS：如果可用季度 < 4，按比例缩放 `raw * (4 / actual_quarters)`
- 现金流：同样的缩放处理
- 标记 `data_quality = "low"` 和 `data_quality = "extrapolated"`

### 新增模型字段（`models.py` HistoricalSnapshot）
- `forward_pe`, `market_pe` — 估值对比
- `data_quality` — "normal"/"low"/"extrapolated"
- `available_years` — 实际可用的年化数据年数

### 修改文件
- `backend/app/agent/backtest/historical_data.py` — F/Z/M Score 计算 + TTM 安全阀 + earnings_growth_10y + market_pe 注入
- `backend/app/agent/backtest/models.py` — 新字段 + to_screening_dict 保留 available_years
- `backend/app/agent/backtest/historical_screener.py` — _adaptive_degrade + _school_consensus 集成
